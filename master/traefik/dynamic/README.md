# Директория для динамических конфигов Traefik
# Файлы .yml здесь создаются автоматически скриптом create-tenant.sh
#
# Формат: <tenant_id>.yml
# Traefik следит за этой папкой (--providers.file.watch=true)
# и подхватывает изменения автоматически
