# ==========================================
# TeamBuh — переменные окружения
# ==========================================
# При копировании папки для нового клиента
# достаточно поменять APP_PORT и INSTANCE_NAME.
# ==========================================

# Внешний порт (уникальный для каждого клиента!)
# Клиент 1: 8001, Клиент 2: 8002, и т.д.
APP_PORT=8001

# Уникальное имя инстанса (для имени контейнера)
INSTANCE_NAME=client1

# API URL для фронтенда (в dev: localhost, в production: пусто)
VITE_API_URL=http://localhost:3001

# Секретный ключ для JWT-токенов (смените на свой!)
JWT_SECRET=change-this-to-random-secret-key
