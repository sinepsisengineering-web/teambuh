# Правила разработки проекта "TeamBuh"

## 📋 Общие правила

### 1. Разделение логики и оформления
- **Логика** (расчёты, данные, бизнес-правила) → файлы в `/services`, `/hooks`, `/utils`
- **Оформление** (UI, стили, компоненты) → файлы в `/components`
- **НЕ СМЕШИВАТЬ** логику и оформление в одном файле

### 2. Унификация функций
- Повторяющиеся функции выносить в общие утилиты
- Избегать дублирования кода в разных файлах
- Единый источник данных (например, праздники — из одного сервиса)

### 3. Структура для многопользовательского режима
- Готовить архитектуру под будущую авторизацию
- Данные должны быть привязаны к пользователю (userId)
- Предусмотреть уровни доступа (admin, user, viewer)

### 4. Выгрузка на GitHub
- Завершённые стабильные этапы сразу выгружать
- Перед выгрузкой: проверить что всё работает
- Коммит с понятным описанием изменений

---

## 🤖 Правила для AI-ассистента

### При отсутствии доступа к командной строке:
1. **Не зацикливаться** на попытках выполнить команды
2. **Сообщить пользователю**: "Мне нужно выполнить команду X, пожалуйста, запустите её"
3. **Объяснять причину**: зачем нужна эта команда
4. **Альтернативы**: предлагать альтернативные решения если возможно

### Формат запроса команды:
```
🖥️ Нужно выполнить команду:
> команда тут

Зачем: краткое объяснение
```

### При ошибках:
- Если команда отменена/не работает → сообщить и предложить альтернативу
- Если видим цикл ошибок → остановиться и объяснить проблему

---

## 📁 Структура проекта

```
teambuh/
├── components/          # UI компоненты (оформление)
├── services/            # Бизнес-логика, API
│   ├── taskGenerator.ts
│   ├── taskRules.ts
│   ├── holidayService.ts  # (будет создан)
│   └── ...
├── hooks/               # React хуки
├── utils/               # Общие утилиты (будет создан)
│   └── dateUtils.ts     # (будет создан)
├── contexts/            # React контексты
├── types.ts             # TypeScript типы
├── constants.ts         # Константы
└── App.tsx              # Главный компонент
```

---

## 🔄 Git Workflow

### Основные команды:
```bash
git status              # Проверить статус
git add .               # Добавить все изменения
git commit -m "..."     # Закоммитить с сообщением  
git push                # Отправить на GitHub
```

### Формат коммитов:
```
feat: добавлена интеграция с API праздников
fix: исправлен расчёт переноса сроков
refactor: унифицированы утилиты дат
docs: обновлена документация
```

---

## ✅ Чеклист перед выгрузкой

- [ ] Приложение запускается без ошибок
- [ ] Новая функция работает корректно
- [ ] Не сломаны существующие функции
- [ ] Код соответствует правилам (логика/оформление разделены)
- [ ] Нет дублирования кода

