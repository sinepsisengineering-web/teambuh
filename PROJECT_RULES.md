# TeamBuh — Правила проекта

> ⚠️ **ЭТИ ПРАВИЛА ОБЯЗАТЕЛЬНЫ ДЛЯ СОБЛЮДЕНИЯ ВО ВСЕЙ РАЗРАБОТКЕ**

---

## 🏗️ Архитектура данных

### Мультитенантность (Multi-tenant)
- **Каждая организация = отдельная изолированная папка**
- Структура: `/server/tenants/{tenantId}/`
- Внутри каждой организации: своя БД, свои файлы, свой ключ шифрования
- Организации НИКОГДА не смешиваются

### Гибридное хранение
| Тип данных | Хранилище |
|------------|-----------|
| Профили (сотрудники, клиенты) | JSON файлы |
| Задачи, зарплата, связи, права | SQLite |
| Документы (сканы) | Зашифрованные файлы в `/vault/` |

### Структура папки организации
```
/tenants/{tenantId}/
├── meta.json           # Метаданные организации
├── db/data.db          # SQLite база
├── employees/          # JSON профили сотрудников
├── clients/            # JSON профили клиентов
├── vault/              # 🔐 Зашифрованные документы
│   └── .key            # Ключ шифрования (AES-256-GCM)
└── backups/            # Резервные копии
```

---

## 🔐 Безопасность

### Документы
- Все сканы хранятся ТОЛЬКО в `/vault/`
- Шифрование: AES-256-GCM
- У каждой организации СВОЙ ключ шифрования
- В SQLite хранятся только метаданные (имя файла, тип, владелец)

### Права доступа
- Гибкая система: роли + индивидуальные разрешения
- Поддержка временных прав (expires_at)
- Права хранятся в SQLite
- Роли: Администратор, Бухгалтер, Помощник бухгалтера (расширяемо)

---

## 🆔 Генерация ID

Формат: `{prefix}_{timestamp}_{random}`

```typescript
// Примеры:
"org_1705680000_x7k2"   // Организация
"emp_1705680000_a1b2"   // Сотрудник
"cli_1705680000_c3d4"   // Клиент
"task_1705680000_e5f6"  // Задача
"doc_1705680000_g7h8"   // Документ
```

ID уникальны ВНУТРИ организации (не глобально).

---

## 🔗 Связи между сущностями

- Все связи через ID (soft references)
- Хранятся в SQLite таблицах
- При удалении связанной сущности — система НЕ падает
- Кэширование имён для быстрого отображения

---

## 📁 Профили (JSON)

### Сотрудник: `/employees/{empId}/profile.json`
```json
{
  "id": "emp_001",
  "personal": { "lastName", "firstName", "email", "phone" },
  "employment": { "type", "workType", "hireDate" },
  "documents": { "passport", "inn", "snils" },  // Данные, НЕ файлы!
  "finance": { "bankName", "bankAccount", "salary", "percent" }
}
```

### Клиент: `/clients/{cliId}/profile.json`
```json
{
  "id": "cli_001",
  "company": { "name", "type", "inn", "kpp" },
  "contact": { "person", "phone", "email" },
  "accounting": { "taxSystem", "monthlyFee" }
}
```

**НЕ хранить в JSON:**
- Файлы документов (→ vault)
- Права доступа (→ SQLite)
- Пароли (→ SQLite auth)
- Связи с другими сущностями (→ SQLite)

---

## 🗄️ SQLite таблицы (в каждой организации)

```
- documents        # Метаданные документов
- roles            # Роли
- permissions      # Разрешения
- role_permissions # Связь роль→разрешения
- user_roles       # Назначенные роли
- user_permissions # Индивидуальные права
- tasks            # Задачи
- payroll_periods  # Зарплатные периоды
- payroll_entries  # Записи зарплат
- employee_clients # Связь сотрудник↔клиент
- client_revenue   # Выручка по клиентам
- audit_log        # История изменений
- auth             # Аутентификация
```

---

## 🌐 API

Все endpoints содержат tenantId:
```
GET  /api/{tenantId}/employees
POST /api/{tenantId}/employees
GET  /api/{tenantId}/clients
...
```

JWT токен содержит: `{ tenantId, userId, role }`

---

## 📦 Миграция организации

Переезд = копирование папки:
```bash
cp -r /server/tenants/org_XXX /new-server/tenants/
```

Никаких дополнительных действий не требуется.

---

## ✅ Чеклист при разработке

При добавлении новой функциональности проверь:

- [ ] Данные привязаны к tenantId?
- [ ] JSON для профилей, SQLite для транзакций?
- [ ] Документы идут в vault с шифрованием?
- [ ] ID генерируется по формату `prefix_timestamp_random`?
- [ ] Связи через ID, не вложенные объекты?
- [ ] Права проверяются через систему permissions?
- [ ] API endpoint содержит `/{tenantId}/`?

---

## 📜 История изменений

### 2026-01-27

**Archive System — Полноценная архивация с документами:**
- Исправлен дубликат Archive API в `server/index.js`
- Архивация клиентов/сотрудников теперь перемещает **всю папку** включая документы
- После восстановления документы возвращаются на место
- Добавлен callback `onRestoreItem` в `ArchiveView` для обновления UI

**Rules System — Справочник правил:**
- Категория правила теперь берётся из **активной папки** при создании
- Добавлен пропс `defaultCategory` в `RuleCreateModal`
- Исправлено обновление `storage_category` при редактировании правила
- Добавлено поле `storageCategory` в данные при сохранении

**Файлы:**
- `server/index.js` — Archive API с перемещением папок
- `server/database/rulesDatabase.js` — обновление storage_category
- `components/RuleCreateModal.tsx` — defaultCategory + storageCategory
- `components/RulesView.tsx` — передача категории в модалку
- `components/ArchiveView.tsx` — onRestoreItem callback
- `App.tsx` — reloadData функция

### 2026-01-26

**Archive System — Базовая реализация:**
- Новый компонент `ArchiveConfirmModal.tsx` — модальное окно подтверждения
- Обновлён `ArchiveView.tsx` — иконка клиентов SVG вместо эмодзи
- `ClientsView.tsx`, `StaffView.tsx` — архивация через `archiveItem`
- `storageService.ts` — функции `archiveItem`, `restoreItem`, `deleteItemForever`, `loadArchive`

**Rules System — Новые компоненты:**
- `RulesView.tsx` — Справочник с папками-категориями
- `RuleCreateModal.tsx` — создание/редактирование правил
- `rulesService.ts` — унифицированный сервис (SQLite API)
- `customRulesService.ts` — работа с кастомными правилами
- `taskRulesService.ts` — сервис правил задач

**Tasks System:**
- `TaskInfoModal.tsx` — модалка информации о задаче
- `TaskModalContext.tsx` — контекст для модальных окон
- Обновления в `TasksView.tsx`, `TaskDetailModal.tsx`, `TaskItem.tsx`
- `taskIndicators.ts` — индикаторы задач

---

*Последнее обновление: 2026-01-27*
